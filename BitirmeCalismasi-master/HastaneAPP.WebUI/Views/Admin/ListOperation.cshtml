@model IEnumerable<Operasyonlar> 


@section Css{
    <link rel="stylesheet" href="//cdn.datatables.net/1.10.22/css/jquery.dataTables.min.css">
}

@section Script{
    <script src="//cdn.datatables.net/1.10.22/js/jquery.dataTables.min.js"></script>
    @* <script>
        $(document).ready( function () {
            $('#userTable').DataTable();
        } );
    </script> *@
}

<h1>Operasyon Listesi</h1>
<hr>
<a asp-controller="Admin" asp-action="CreateOperation" class="badge badge-primary mb-3">Yeni Operasyon</a>

<table id="userTable" class="table table-bordered">
    <thead>
        <tr>
            
           
            <td>Id</td>
            <td>İsim</td>
            

            <td style="width: 150px;"></td>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count() > 0)
        { 
            @foreach (var tmp in Model)
            {
                <tr>
                    <td>@tmp.Id</td>
                    <td>@tmp.Name</td>
                  
                </tr>
            }
        }
         
        
    </tbody>
</table>